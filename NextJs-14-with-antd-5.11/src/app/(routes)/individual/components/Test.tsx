"use client";

type Props = {};

const Test = (props: Props) => {
  return (
    <div>
      {/* <Button primary label={"zzzz"} onClick={() => alert("hello")} /> */}
    </div>
  );
};

export default Test;
